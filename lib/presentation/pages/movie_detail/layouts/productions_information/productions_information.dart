part of '../../movie_detail.dart';

class _ProductionsInformation extends StatelessWidget {
  const _ProductionsInformation({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Text('Productions Information'),
    );
  }
}